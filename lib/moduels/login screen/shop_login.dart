import 'package:flutter/material.dart';

class ShopAppLogin extends StatelessWidget {
  const ShopAppLogin({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Login Screen'),
      ),
    );
  }
}
